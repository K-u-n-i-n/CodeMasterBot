# Тестовые переменные окружения для CI/CD

# Django settings
DJANGO_SECRET_KEY=test-secret-key-for-ci-cd-not-for-production
DEBUG=False

# Database settings (используется SQLite в памяти для тестов)
POSTGRES_DB=test_db
POSTGRES_USER=test_user
POSTGRES_PASSWORD=test_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Telegram settings (тестовые токены)
TELEGRAM_TOKEN=123456789:test-token-for-ci-cd-testing
TELEGRAM_FAKE_TOKEN=987654321:fake-token-for-testing

# Redis settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Другие настройки
ALLOWED_HOSTS=localhost,127.0.0.1,testserver
CSRF_TRUSTED_ORIGINS=http://localhost,http://127.0.0.1
